import React from "react";
import {
	AppRegistry
} from "react-native";
import App from "./src";

const ReactNativeResponsive = (props) => {
	return (
		<App />
	);
};

AppRegistry.registerComponent("ReactNativeResponsive", () => ReactNativeResponsive);
